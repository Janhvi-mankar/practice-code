#include <vector>
using namespace std;

vector<int> runningSum(vector<int>& nums)
{
    vector<int> ans;
    int tmpAns = 0;

    int sz = nums.size();

    for (int i = 0; i < sz; i++)
    {
        tmpAns += nums[i];
        ans.push_back(tmpAns);
    }

    return ans;
}
