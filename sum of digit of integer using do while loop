#include <stdio.h>
int main()
{
   int sum = 0,cnt = 0;
   int n ;
   
   do
   {
      printf("enter a number:");
      scanf("%d",&n);

      if (n!=0) 
      cnt++;
      sum=sum+n;

    }
    while(n!=0||cnt==0);
        printf("sum=%d",sum);
        scanf("cnt=%d",cnt);
    return 0;
}
