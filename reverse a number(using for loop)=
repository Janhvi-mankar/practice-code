#include <stdio.h>

int main()
{
    int n = 0;
    printf("Enter n: ");
    scanf("%d", &n);

    int rev = 0;

    while (n > 0)
    {
        int tmp = n % 10;
        rev = (rev * 10) + tmp;
        n = n / 10;
    }

    printf("Reverse = %d\n", rev);

    return 0;
}
