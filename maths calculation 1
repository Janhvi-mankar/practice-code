#include <stdio.h>
#include<math.h>
int main()
{
    int x;
    printf("enter x value:");
    scanf("%d",& x );
    double y = sqrt (( pow(x,5)+pow(x,5)+pow(x)+1)/2);
    printf("y is = % f",y);
    return 0;
}
